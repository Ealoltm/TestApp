# Basic credentials (safe method)
adminUser: admin
adminPassword: admin123

# Ensure Helm doesn’t try to use any existing secret
admin:
  existingSecret: ""

# Optional environment variables for clarity
env:
  GF_SECURITY_ADMIN_USER: admin
  GF_SECURITY_ADMIN_PASSWORD: admin123

# (Optional) Configure Grafana defaults
grafana.ini:
  users:
    default_theme: light
  server:
    root_url: http://grafana.local
