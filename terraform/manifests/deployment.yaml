apiVersion: apps/v1
kind: Deployment
metadata:
  name: mini-store
  namespace: default
  labels:
    app: mini-store
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mini-store
  template:
    metadata:
      labels:
        app: mini-store
    spec:
      containers:
      - name: mini-store
        image: ghcr.io/ealol/testapp:v1.0.0
        ports:
        - containerPort: 8000